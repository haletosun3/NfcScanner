/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Wrapper from './NfcScanner/Wrapper';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Wrapper);
